class Dot{
    constructor(){
        this.position = createVector();
    }

    show(){
        stroke(0,255,0);
        strokeWeight(3);
        //point(this.position.x, this.position.y);
    }    
}